#!/bin/bash
hashcat --stdout wordlist1.txt wordlist2.txt | hashcat -m 0 -a 0 "$1" --quiet 2>/dev/null || true && printf '<%s>\n' "$(hashcat --show -m 0 "$1" 2>/dev/null | awk -F: 'NF>=2{print $NF}' | paste -sd, - )" > 9-password.txt